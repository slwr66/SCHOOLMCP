# =============================================================================
# MCP Server для EdTech / Lesson Kit
# Пример файла переменных окружения
# 
# Инструкция:
# 1. Скопируйте этот файл как .env
# 2. Заполните необходимые значения
# 3. Убедитесь, что .env добавлен в .gitignore (уже добавлен)
# =============================================================================

# =============================================================================
# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ
# =============================================================================

# Unsplash API (для get_images)
# Получить: https://unsplash.com/developers
UNSPLASH_ACCESS_KEY=your_unsplash_access_key_here

# Yandex Translate API (для get_quiz — перевод вопросов)
# Получить: https://console.cloud.yandex.ru/

# Вариант 1: API Key (проще, рекомендуется)
YANDEX_API_KEY=your_yandex_api_key_here

# Вариант 2: IAM Token (требует folder_id)
# YANDEX_IAM_TOKEN=your_yandex_iam_token_here
# YANDEX_FOLDER_ID=your_yandex_folder_id_here

# =============================================================================
# ОПЦИОНАЛЬНЫЕ ПЕРЕМЕННЫЕ
# =============================================================================

# Google Calendar API (для schedule_lesson)
# Получить: https://console.cloud.google.com/
# Требуется OAuth 2.0 Client ID (Desktop App)
GOOGLE_CREDENTIALS_PATH=credentials.json
GOOGLE_TOKEN_PATH=token.json
GOOGLE_CALENDAR_ID=primary

# Aspose Slides (для create_presentation)
# Без лицензии презентации будут содержать watermark "Evaluation Only"
# Приобрести: https://purchase.aspose.com/
# ASPOSE_LICENSE_PATH=path/to/Aspose.Slides.lic

# Директория для экспортированных файлов
# По умолчанию: exports
EXPORTS_DIR=exports

# =============================================================================
# MCP СЕРВЕР КОНФИГУРАЦИЯ
# =============================================================================

# Режим работы MCP сервера:
# - "stdio" - локальный режим через standard input/output (для тестирования)
# - "sse" - HTTP/SSE режим для удалённого подключения (Cloud.ru, продакшен)
MCP_MODE=sse

# Тип транспорта для HTTP режима (только если MCP_MODE=sse):
# - "sse" - Server-Sent Events (SSE) - рекомендуется для MCP агентов
# - "http" - обычный HTTP (альтернатива)
MCP_TRANSPORT=sse

# Хост для HTTP сервера (обычно 0.0.0.0 для Docker)
HOST=0.0.0.0

# Порт для HTTP сервера
PORT=8000

# =============================================================================
# DOCKER ПЕРЕМЕННЫЕ (автоматически устанавливаются в Dockerfile)
# =============================================================================

# Python unbuffered mode (для логирования в Docker)
# PYTHONUNBUFFERED=1

# =============================================================================
# ПРИМЕЧАНИЯ
# =============================================================================

# 1. OpenTDB API и Wikipedia API не требуют ключей
# 2. Для Google Calendar при первом запуске откроется браузер для авторизации
# 3. Все credentials файлы (*.json, *.lic) должны быть в .gitignore
# 4. В продакшене используйте систему секретов Cloud.ru или Docker Secrets

